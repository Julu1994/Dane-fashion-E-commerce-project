import "./input.scss";
import React from "react";

const Input = ({}) => {
    return (
        <div className="input">
            <input className="input-field" type="text" />
            <label>{props}</label>
        </div>
    );
};

export default Input;
